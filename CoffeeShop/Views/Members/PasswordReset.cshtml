<head>
    <style>
        html, body {
            height: 100%; /* 保持頁面全高 */
            margin: 0;
        }

        .main-container {
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .form-container {
            width: 100%; /* 保持表單的寬度彈性 */
            padding: 20px; /* 增加表單內的間距 */
            border: 1px solid #505637; /* 添加外框 */
            border-radius: 10px; /* 圓角效果 */
            box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.1); /* 添加陰影以使外框更明顯 */
            margin: 50px; /* 四周增加 50px 的空白 */
            text-align: center; /* 文字及表單元素居中 */
        }

        .form-label {
            font-size: 16px;
        }

        .form-control {
            width: 100%; /* 確保輸入框在小屏幕上也能適應 */
            max-width: 300px; /* 限制輸入欄的寬度，減少過長的輸入框 */
            border: none; /* 移除內層外框 */
            padding: 5px; /* 增加輸入框的內間距 */
            border-bottom: 1px solid #505637; /* 增加底部邊框 */
            margin: 0 auto; /* 確保輸入框在容器內水平居中 */
        }

        h2 {
            text-align: center; /* 標題居中 */
        }

        .p-5 {
            padding: 3rem !important;
            margin-top: 200px;
        }

        #footer {
            position: fixed !important;
            bottom: 0 !important;
            width: 100% !important;
            background-color: #505637 !important;
            margin: 0 !important;
            padding: 10px 0 !important;
            text-align: center;
            z-index: 1000; /* 確保它在最上層 */
        }

        .btn1 {
            width: 150px;
            display: inline-block;
        }
    </style>
</head>

<body>
    <div class="container-fluid main-container">
        <div class="right-container p-5">
            <div class="form-container">
                <h2>密碼重置</h2>
                <div class="row poppins-regular justify-content-center">
                    <hr class="col-8">
                    <div class="col-8 position-relative">
                        <div class="row" style="margin: auto;">
                            <!-- 新密碼欄位 -->
                            <div class="col-12 m-1">
                                <label class="form-label" for="newPassword">輸入新密碼</label>
                                <input type="password" id="newPassword" class="form-control">
                            </div>
                            <!-- 確認新密碼欄位 -->
                            <div class="col-12 m-1">
                                <label class="form-label" for="confirmPassword">確認新密碼</label>
                                <input type="password" id="confirmPassword" class="form-control">
                            </div>
                            <!-- 送出按鈕 -->
                            <div class="col-12 m-1 text-center">
                                <button class="btn1" onclick="resetPassword()">送出</button>
                            </div>

                            <script>
                                // 送出按鈕點擊事件
                                function resetPassword() {
                                    const newPassword = document.getElementById('newPassword').value;
                                    const confirmPassword = document.getElementById('confirmPassword').value;

                                    if (newPassword !== confirmPassword) {
                                        Swal.fire({
                                            title: '錯誤',
                                            text: '密碼不一致，請重新確認！',
                                            icon: 'error',
                                            confirmButtonText: '確定'
                                        });
                                    } else {
                                        Swal.fire({
                                            title: '密碼重置成功',
                                            text: '您的密碼已成功更新！',
                                            icon: 'success',
                                            confirmButtonText: '確定'
                                        }).then((result) => {
                                            if (result.isConfirmed) {
                                                // 跳轉到登入頁面
                                                window.location.href = '/Members/Login';
                                            }
                                        });
                                    }
                                }
                            </script>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>

@section styles{
    <link href="~/Content/style.css" rel="stylesheet" />
}

<script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
